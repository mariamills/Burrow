// HTMX v2.0 — Download from https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js
// Place the minified HTMX library here (~14KB gzipped).
// For development, you can use the CDN by replacing the script tag in layout.html with:
// <script src="https://unpkg.com/htmx.org@2.0.4"></script>
//
// This placeholder provides no-op stubs so the UI loads without errors.
(function(){
  // Minimal HTMX stub — replace with real htmx.min.js for full functionality.
  console.log('HTMX placeholder loaded. Replace with real htmx.min.js for dynamic features.');
})();
